# Poker Ledger — trang tĩnh (build)

Các file ở đây được DỰNG TỰ ĐỘNG từ repo riêng `PokerLedger` (`node dev-tools/publish-static.js`).
Đừng sửa tay ở đây — sửa ở repo gốc rồi publish lại.

**Cài lên iPhone:** mở link bằng Safari → Chia sẻ → Thêm vào MH chính.
